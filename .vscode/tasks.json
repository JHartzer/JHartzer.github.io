{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Jekyll Build/Serve",
            "type": "shell",
            "command": "bundle exec jekyll build && bundle exec jekyll serve --watch",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}